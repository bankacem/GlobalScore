name: Refresh football data

on:
  workflow_dispatch:
  schedule:
    # Generate and publish the daily multilingual editorial set at 05:17 UTC.
    - cron: "17 5 * * *"

permissions:
  contents: write

concurrency:
  group: refresh-football-data
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the Pages branch
        uses: actions/checkout@v4
        with:
          ref: feature/globalscore-phase1-foundation-6374903721955978638

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch fixtures and player headlines
        run: python scripts/update_data.py

      - name: Generate today and tomorrow articles
        run: python scripts/generate_multilingual_articles.py

      - name: Generate permanent match, team and competition pages
        run: python scripts/generate_permanent_pages.py

      - name: Generate search and player discovery pages
        run: python scripts/generate_discovery_pages.py

      - name: Commit the refreshed snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ar en fr es assets/data/content.json assets/data/live.json assets/data/article_registry.json scripts/generate_multilingual_articles.py scripts/generate_permanent_pages.py scripts/generate_discovery_pages.py scripts/update_data.py assets/js/match-page.js assets/js/search-page.js assets/css/main.css sw.js Sitemap.xml sitemap.xml sitemap-index.xml
          if git diff --cached --quiet; then
            echo "No data changes to commit."
          else
            git commit -m "chore: refresh football data and articles"
            git push origin HEAD:feature/globalscore-phase1-foundation-6374903721955978638
          fi
